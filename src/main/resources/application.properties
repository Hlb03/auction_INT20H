spring.datasource.url=jdbc:postgresql://${PROVIDE_YOUR_OWN_HOST}
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# The format used for the keystore. It could be set to JKS in case it is a JKS file
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=classpath:keystore/auction.p12
# The password used to generate the certificate
server.ssl.key-store-password=
# The alias mapped to the certificate
server.ssl.key-alias=


server.port=8080
server.error.include-message=always

spring.mvc.servlet.path=/auction/api

#For local development purposes only
#spring.jpa.show-sql=true

#Local development as well
#file.path=C:/Users/gfiet/hackaton/
file.path=${FILE_PATH}